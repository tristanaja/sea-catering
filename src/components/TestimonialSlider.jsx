import React, { useState } from 'react';const TestimonialSlider = ({ testimonials }) => {  const [currentIndex, setCurrentIndex] = useState(0);  const goToNext = () => {    setCurrentIndex((prevIndex) => (prevIndex + 1) % testimonials.length);  };  const goToPrevious = () => {    setCurrentIndex((prevIndex) =>      prevIndex === 0 ? testimonials.length - 1 : prevIndex - 1    );  };  if (!testimonials || testimonials.length === 0) {    return <p className="text-center text-[#333333] text-xl font-['Lora']">No testimonials yet.</p>;  }  const currentTestimonial = testimonials[currentIndex];  return (    <div className="relative bg-[#FDF8F5] p-8 rounded-lg shadow-lg font-['Lora']">      <h2 className="text-3xl font-bold text-center mb-6 font-['Montserrat'] text-[#333333]">What Our Customers Say</h2>      <div className="text-center">        <p className="text-xl italic text-[#333333] mb-4">\"{currentTestimonial.reviewMessage}\"</p>        <p className="text-lg font-semibold text-[#E85A4F]">- {currentTestimonial.customerName}</p>        <div className="text-yellow-500 text-xl mt-2">          {'★'.repeat(currentTestimonial.rating)}{'☆'.repeat(5 - currentTestimonial.rating)}        </div>      </div>      <button        onClick={goToPrevious}        className="absolute left-4 top-1/2 transform -translate-y-1/2 bg-[#E85A4F] text-white p-2 rounded-full shadow-md hover:bg-red-700 transition-colors duration-300"      >        &lt;      </button>      <button        onClick={goToNext}        className="absolute right-4 top-1/2 transform -translate-y-1/2 bg-[#E85A4F] text-white p-2 rounded-full shadow-md hover:bg-red-700 transition-colors duration-300"      >        &gt;      </button>    </div>  );};export default TestimonialSlider;